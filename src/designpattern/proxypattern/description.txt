代理模式: 优点:在不修改源代码的情况下对目标对象的功能进行增强
分为两种：
静态代理：优点效率比动态代理高,代理类必须提前写出,代理类委托类必须实现同一接口，
一旦接口发生改变委托类代理类都需要修改源码。

动态代理：发生在程序运行阶段，运行时效率受到影响,好处动态生成代理类
jdk代理：运行中生产代理类缺点目标对象必须实现接口，运用了反射生成了一个实现代理接口的匿名类;
cglib代理：要求目标类不能是final,目标类方法不能用 final/static修饰，走的是继承思路采用ASM修改字节码文件技术
生成子类来产生代理类.