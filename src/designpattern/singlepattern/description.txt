  单例模式特点：私有化构造器 自己创建实例 并且返回实例
  好处：控制整个应用程序中只有一个实例.减少内存开销。
  缺点：单例没有抽象层，扩展性差。长时间对象空闲，会被垃圾回收，造成对象状态的丢失
  常见三种实现方式：
  饿汉模式
  优点：线程安全
  缺点：容易产生垃圾  (发生在类装载的初始化阶段) 类装载分为三个过程 加载  链接(校验 准备 解析)  初始化
  懒汉模式:
  优点：(延时加载)在调用的时候才会实例化
  缺点：线程不安全
  双重检查模式：
  优点:线程安全 不会产生垃圾对象
  (第一次判断是为了多个线程同时被阻塞;第二次是为了防止对象重复创建)